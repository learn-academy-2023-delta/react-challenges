import React from 'react'

const DiceBox = (props) => {
  return (
    <div className = "box">
      <h3>DiceBox {props.title}</h3>
    </div>
  )
}

// We need to make three boxes using css and the components script
// We need to put an image into that box that will act as a button
// We need to set up a random num generator 1-6
// 
// 